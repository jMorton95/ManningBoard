version: '3.1'

services:
  db:
    image: postgres:latest
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports: 
      - '7003:5432'
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
